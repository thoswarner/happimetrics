<div class="row-fluid">
  <div class='span12'>

      <div class='row-fluid title-block'>
        <div class='align-right span4'>
          <%= link_to "Previous week", other_week_path(current_beginning_of_week_day, :previous), :class => "btn" if current_beginning_of_week_day %>
        </div>
        <div class='center span4'>
           <h1><h2><%= current_beginning_of_week_day.strftime("%A, %d %B %Y") %> to <%= current_beginning_of_week_day.end_of_week.strftime("%A, %d %b %Y") %> </h2></h1>
        </div>
        <div class='align-left span4'>
          <%= link_to "Following week", other_week_path(current_beginning_of_week_day, :next), :class => "btn" if current_beginning_of_week_day %>
        </div>      
      </div>

    <% if entries.any? %>
      <%= render 'shared/happiness_overview' %>
      <%= render 'shared/metrics', :type => type %>
      <%= render 'shared/graph', :entries => entries, :type => type %>
    <% else %>
      <div class='row-fluid'>
        <div class='center span12'>
          <h3>Sorry! There are no happiness entries available for week you have chosen.</h3>
        </div>    
      </div>
    <% end %>

  </div>

</div>